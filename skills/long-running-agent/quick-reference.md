# 快速参考卡

## 核心原理

```
Token 使用量解释 80% 的性能方差
多代理性能提升 90.2%
```

## 核心机制

```
任务 → 开发 → 测试 → 审查 → 完成 → Git提交
       ↑              ↓
       └──── 不通过 ←─┘
```

## Git 自动化

```
任务开始 → 自动 commit "开始: xxx"
任务完成 → 自动 commit "完成: xxx"
文件修改 → 自动 commit "更新: xxx"
会话结束 → 自动 commit "进度: 保存"
```

无需手动操作 git，Agent 自动处理。

## 必须有的角色

| 角色 | 职责 |
|------|------|
| coder | 写代码，请求审查 |
| reviewer | 验证，批准/拒绝 ← 必须！ |
| tester | 运行测试 |

## 五大工作流模式

| 模式 | 适用场景 |
|------|----------|
| Prompt Chaining | 固定步骤任务 |
| Routing | 分类导向处理 |
| Parallelization | 可并行子任务 |
| **Orchestrator-Workers** | 复杂不可预测任务 ⭐ |
| **Evaluator-Optimizer** | 需要迭代改进 ⭐ |

## 任务模板

```markdown
## 完成标准
- [ ] 功能点 1
- [ ] 功能点 2
- [ ] 测试通过

## 验证命令
npm test
```

## 完成流程

```bash
# 1. Coder 完成工作
npm test  # 必须先测试

# 2. 请求审查
SendMessage(recipient: "reviewer", content: "任务 #X 请求审查...")

# 3. 等待回复

# 4. 通过后才标记完成
TaskUpdate(taskId: "X", status: "completed")
```

## Token 效率原则

```
1. 分页获取大数据
2. 压缩输出返回摘要
3. 多次小搜索优于单次大搜索
4. 单次输出限制 25,000 tokens
```

## 五不原则

| 原则 | 含义 |
|------|------|
| 不做完不结束 | 每个任务必须有明确完成标准 |
| 不验证不完成 | 必须有人审查 |
| 不记录不结束 | 必须写入进度 |
| 不恢复不开始 | 新会话必须检查上次进度 |
| 不提交不结束 | 每次变更必须 git commit |

## 会话恢复

```bash
# 每次新会话
1. Read progress.json
2. TaskList
3. 恢复未完成任务
```

## 关键数字

| 指标 | 数值 |
|------|------|
| Token 使用解释方差 | 80% |
| 多代理性能提升 | 90.2% |
| 工具响应限制 | 25,000 tokens |
| 评估最小样本 | ~20 个 |